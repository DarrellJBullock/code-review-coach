{
  "name": "@code-review-coach/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared enums, zod schemas, and DTO contracts for PR Review Coach (framework-agnostic).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
